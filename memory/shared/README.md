# Shared zone

Сюда кладётся только то, что один из владельцев **явно** разрешил шерить с другими.

В каждом shared-файле должен быть заголовок:

```
owner: <кто создал>
shared-with: [<список>]
since: YYYY-MM-DD
note: <комментарий>
```
